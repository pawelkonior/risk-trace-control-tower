apiVersion: v1
kind: Service
metadata:
  name: rwa-frontend
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rwa-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
    protocol: TCP
    name: http
  selector:
    {{- include "rwa-frontend.selectorLabels" . | nindent 4 }}
