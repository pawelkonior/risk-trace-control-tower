apiVersion: v1
kind: Namespace
metadata:
  name: rwa-dev
  labels:
    environment: dev
    team: rwa
    regulatory: bcbs239-dora
    kubernetes.io/metadata.name: rwa-dev
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: rwa-dev-quota
  namespace: rwa-dev
spec:
  hard:
    requests.cpu: "8"
    requests.memory: 16Gi
    limits.cpu: "32"
    limits.memory: 64Gi
    pods: "50"
---
apiVersion: v1
kind: LimitRange
metadata:
  name: rwa-dev-limitrange
  namespace: rwa-dev
spec:
  limits:
  - type: Container
    default:
      cpu: "2"
      memory: 2Gi
    defaultRequest:
      cpu: 100m
      memory: 128Mi
