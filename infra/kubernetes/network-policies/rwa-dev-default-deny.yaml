# Zero-trust default: deny ALL ingress and egress for every pod in rwa-dev.
# This MUST be applied before any allow rules.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: rwa-dev
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  - Egress
